---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - name: {{ .Release.Name }}
    port: {{ .Values.containerPort }}
  selector:
    app: {{ .Release.Name }}
  type: ClusterIP
...
